package Chapter8;

interface IAnimal { // dung interface -> k co ham tao
	public void animalSound(); // interface method (does not have a body)

	public void run(); // interface method (does not have a body)
}
